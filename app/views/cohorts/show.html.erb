<h1>Cohort graduates</h1>

<ul>
  <% @cohort.profiles.each do |profile| %>
    <li> <%= link_to profile.name, "/profiles/#{profile.id}" %> 
    </li>
  <% end %>
</ul>

<%= link_to 'Cohort_page', '/cohorts' %>
<%= link_to 'User_page', '/show' %>
